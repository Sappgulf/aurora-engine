//! Aurora platformer demo library: headless simulation, procedural art,
//! persistence, and UI kit. The binary in `main.rs` is the playable shell;
//! the CI bot and tooling binaries drive the same library.

pub mod art;
pub mod game_core;
pub mod save;
pub mod ui_kit;
